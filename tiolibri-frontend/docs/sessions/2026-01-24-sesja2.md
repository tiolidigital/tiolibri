# Sesja 2 - 2026-01-24

## Podsumowanie sesji

**Cel:** Zbudowanie bazowych komponentów UI

**Co zrobiono:**
- Button (4 warianty: primary, secondary, ghost, danger + loading state)
- Input + Textarea (z error handling, focus ring, disabled state)
- Card (hover effects, padding variants)
- Badge (3 status variants: draft, in_progress, completed + kolory)
- Modal (overlay + ESC handling + click outside to close)

**Co zostało:**
- Auth flow (LoginPage, SignupPage, useAuth hook)
- Dashboard (lista projektów)
- Project Editor (upload HTML, TipTap, zarządzanie rozdziałami)
- Python API backend

---

## Zmiany w kodzie

### Frontend (tiolibri-frontend/)

**Nowe pliki:**
- `src/components/ui/Button.jsx` - przycisk z wariantami i loading state
- `src/components/ui/Input.jsx` - input z error handling
- `src/components/ui/Textarea.jsx` - textarea z error handling
- `src/components/ui/Card.jsx` - karta z hover effect
- `src/components/ui/Badge.jsx` - badge dla statusów projektów
- `src/components/ui/Modal.jsx` - modal z overlay i ESC handling

**Zmodyfikowane pliki:**
- `src/App.jsx` - test page dla wszystkich komponentów UI

---

## Decyzje architektoniczne

### TipTap Core jako WYSIWYG Editor
- MIT license (FREE, komercyjne użycie OK)
- Lightweight (~100KB)
- React-native API
- Dodano wpis do DECISIONS.md

---

## Znane bugi / TODO

- [ ] App.jsx = test page - trzeba zamienić na routing (Sesja 3)
- [ ] Komponenty gotowe - można budować features (Auth, Dashboard)

---

## Kontekst dla następnej sesji

**Zacząć od:** Auth flow
1. `src/features/auth/useAuth.js` - hook do logowania/rejestracji
2. `src/features/auth/LoginPage.jsx` - strona logowania
3. `src/features/auth/SignupPage.jsx` - strona rejestracji
4. Protected routes w React Router

**Gotowe do użycia:**
- Supabase Auth jest skonfigurowany
- Komponenty UI (Button, Input, Card, Modal) gotowe
- Supabase client w `src/lib/supabase.js`

**Instalacja TipTap (gdy będziemy budować Editor):**
```bash
npm install @tiptap/react @tiptap/starter-kit
```

---

*Czas sesji: ~30 min*
*Następna sesja: Auth flow + Protected routes*
