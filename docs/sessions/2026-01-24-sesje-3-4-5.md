# Session Log: 2026-01-24 (Sesje 3, 4, 5)

## Podsumowanie sesji

**Cel:** Zbudowanie kompletnego MVP - Auth + Dashboard + Editor

### Sesja 3 - Auth Flow
- LoginPage, SignupPage (z email confirmation)
- useAuth hook + AuthProvider (context)
- ProtectedRoute component (HOC)
- React Router 7 routing (/login, /signup, /dashboard)

### Sesja 4 - Dashboard CRUD
- useProjects hook (fetch, create, update, delete)
- NewProjectModal (title, author, language dropdown)
- ProjectCard component (grid layout, status badges)
- Empty state dla nowych użytkowników
- timeAgo() utility function

### Sesja 5 - Project Editor
- EditorPage (3-column layout: metadata | editor | styles)
- ChapterUpload (drag & drop HTML → Supabase Storage)
- useChapters hook (fetch, upload, delete, getContent)
- ChapterList (clickable items z delete confirmation)
- TipTap WYSIWYG editor (StarterKit: headings, bold, italic, lists)
- EditorToolbar (H1-H3, B, I, S, lists, quote, undo/redo)
- Auto-save (2s debounce → processed_html)
- Style selector (3 presety: Classic, Modern, Minimal)

### Co zostało
- Python Backend (FastAPI + ebooklib + WeasyPrint)
- Deploy (Vercel frontend + Railway backend)

---

## Zmiany w kodzie

### Frontend (tiolibri-frontend/)

**Sesja 3 - Auth:**
```
src/features/auth/
├── useAuth.jsx          # Hook + AuthProvider (signIn, signUp, signOut)
├── LoginPage.jsx        # Formularz logowania
├── SignupPage.jsx       # Formularz rejestracji + email confirmation
├── ProtectedRoute.jsx   # HOC - redirect do /login
└── index.js             # Eksporty modułu

src/features/projects/
└── DashboardPage.jsx    # Placeholder (rozbudowany w Sesji 4)

src/App.jsx              # Dodany React Router + AuthProvider
```

**Sesja 4 - Dashboard:**
```
src/features/projects/
├── useProjects.js       # CRUD hook (fetchProjects, createProject, deleteProject)
├── NewProjectModal.jsx  # Modal tworzenia projektu
├── ProjectCard.jsx      # Karta projektu (title, author, badge, actions)
└── DashboardPage.jsx    # Lista projektów + empty state

src/lib/utils.js         # Dodana funkcja timeAgo()
```

**Sesja 5 - Editor:**
```
src/features/editor/
├── useChapters.js       # CRUD rozdziałów + Supabase Storage
├── EditorPage.jsx       # Layout 3-kolumnowy
├── ChapterUpload.jsx    # Drag & drop HTML files
├── ChapterList.jsx      # Lista rozdziałów (select, delete)
├── ChapterEditor.jsx    # TipTap integration + auto-save
├── EditorToolbar.jsx    # Toolbar z przyciskami formatowania
└── editor.css           # Style typograficzne dla TipTap

src/App.jsx              # Dodany route /editor/:projectId
```

### Dependencies dodane
```json
{
  "react-router-dom": "^7.1.3",
  "@tiptap/react": "^2.x",
  "@tiptap/starter-kit": "^2.x",
  "@tiptap/pm": "^2.x"
}
```

---

## Zmiany w Supabase

- Brak zmian strukturalnych (wszystko z Sesji 1-2)
- **Bug fix:** zmiana nazwy bucketa z `'chapters'` na `'uploads'` w useChapters.js

---

## Znane bugi / TODO

- [ ] Generate button (disabled - backend nie gotowy)
- [ ] Drag & drop reordering rozdziałów (v1.1)
- [ ] Device preview modes (v1.1)
- [ ] Python API (następna sesja)
- [ ] Deploy na Vercel
- [ ] Chunk size warning (TipTap ~800KB) - rozważyć code splitting

---

## Kontekst dla następnej sesji

**Zacząć od:** Python Backend (FastAPI + ebooklib + WeasyPrint)

**Stan projektu:**
- MVP Frontend jest kompletny - można testować pełny flow (minus generowanie)
- TipTap działa z auto-save - użytkownik może edytować literówki bez re-exportu z Google Docs
- Supabase Storage działa - pliki HTML uploadowane do bucketa `uploads`

**Architektura API (planowana):**
```
POST /generate
Request: { project_id, formats: ["epub", "pdf"], style_preset }
Response: { files: { epub: url, pdf: url }, stats: { chapters, images, time } }
```

**Technologie do użycia:**
- FastAPI (Python web framework)
- ebooklib (generator EPUB)
- WeasyPrint (generator PDF)
- supabase-py (client do Supabase)
