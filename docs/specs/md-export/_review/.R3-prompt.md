Cześć Codex. R3 review docs/specs/md-export/SPEC-MD-EXPORT.md (wersja 0.4.1, status R3-codex-pending).

Przeczytaj NAJPIERW (w tej kolejności):
1. /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/SPEC-WORKFLOW-MASTER.md (sekcje 4.1, 4.4a, 4.4b, 4.6, 4.7, 9). CME-MANIFEST.md NIE ISTNIEJE w żadnym repo — zgodnie z regułą 1 audyt C/M/E nie jest blokerem, ale bramka strukturalna preflightu obowiązuje.
2. /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/RETRO.md
3. docs/specs/md-export/SPEC-MD-EXPORT.md
4. docs/specs/md-export/_review/R1-codex.md, R2-codex.md, R1-opus-response.md, R2-opus-response.md, R2-opus-preflight.md oraz R3-opus-preflight.md (preflight tej rundy — zawiera dowody uruchomieniowe, w tym przebieg PRAWDZIWEGO segmentuj())
5. docs/specs/md-export/PROACTIVE-INBOX.md

KONTEKST BUDŻETU — TO JEST OSTATNIA RUNDA. N_EFF=2 = MAX_ROUNDS (Risk STANDARD), przedłużenie konwergencji zostało już przyznane i zapisane w STATE (`convergence-ext: R2`). Kolejnego nie ma. R3 musi wyjść GREEN (APPROVE / NITS / NITS-EXT) albo spec idzie do ESCALATED.

CO SIĘ ZMIENIŁO OD R2 (v0.3.1 → v0.4 → v0.4.1) — wszystkie Twoje pięć uwag z R2 przyjęte, żadna nie odrzucona:
- #1/#2 (aparatura C/M/E): trzy rekordy z zawężonym C (trzeci znalazł sweep, nie Ty), rekord CONTRACTED przebudowany. UWAGA: w R3 cofnąłem formę poprawki #2 — dopisane w R2 pole `E` do rekordu CONTRACTED łamie regułę 5 (rekord był MEASURED i CONTRACTED naraz, bramka `cme_typ_both` to odrzuca). Uruchomiona część C jest teraz niesiona przez dwa osobne rekordy MEASURED. Sprawdź, czy to rozwiązanie faktycznie domyka Twój bloker #2, czy tylko przenosi go gdzie indziej.
- #3 (sprzeczność G2): G2 = `kod == 0` ∧ `tabela == 0`; `lista` wypadła z G2 i została wyłącznie w G1.
- #4 (sygnatura endpointu): wpisana `request: Optional[ExportMdRequest] = None`, zwalidowana wobec produkcji (export_import.py:21,34-38) i URUCHOMIONA na TestClient z kontrpróbą (bez Optional → 422 na brak body). PASS=6 FAIL=0, EXIT=0.
- #5 (semantyka `blocks`): nowy podrozdział §Jak liczymy `blocks` + tabela 9 konsekwencji + 8 fixture'ów w kroku 1 planu.
- v0.4 → v0.4.1 (korekty preflightu R3, moje, przed handoffem): kolejność gałęzi algorytmu `blocks` poprawiona na kolejność KONSUMENTA (fence → ATX → tabela → BQ → marker) — stara kolejność była obserwowalnie innym algorytmem, zmierzone na F10/F11; SHA kanonu konsumenta (`134f8e4`, plik bajtowo identyczny w trzech rewizjach); dwa stale-refy liczbowe (`~3×` → `3,44×`, `~67 LOC` → `~82 LOC`).

Sizing: dyspensa urosła ~67 → ~82 LOC (5 plików / ~582 LOC wobec limitu 500/90min/5), źródło autoryzacji = decyzja właściciela z R1, liczba nowych przypadków testowych zapisana wprost (8).

Zadanie: Review PRZED implementacją. Wylicz WSZYSTKIE problemy TEJ rundy naraz. Widzisz warstwę B pod A — zgłoś OBIE teraz.
Przejdź exhaustiveness checklist. Dla KAŻDEJ kategorii [✓ sprawdzone] albo [N/A — jedno zdanie].
- [ ] budżet ROZMIARU (LOC/pliki/domeny/migracje — NIE liczba testów; §4.5)        ← ZAWSZE
- [ ] spójność z masterem + brak stale references                                   ← ZAWSZE
- [ ] egzekwowalność (spec → impl bez zgadywania?)                                   ← ZAWSZE
- [ ] poprawność logiki + edge/nullish        ← jeśli algorytm/logika
- [ ] bundling / mutation targets (1 it()=1 gałąź?)   ← TYLKO pure-logic test spec
- [ ] typy/sygnatury/argi/ścieżki istnieją    ← jeśli cytuje symbole/ścieżki
- [ ] bramki maszynowe URUCHOMIONE w sandboxie (komendy/regexy/fixture wykonane, podaj EXIT — NIE weryfikuj flag wobec docs; nieuruchamialna albo niewykonana bramka = RC). Konkretnie: masz w sandboxie Node 24 i venv `tiolibri-api/venv` — możesz odpalić PRAWDZIWY `segmentuj()` z /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA-redaktor (gałąź `redaktor`) i sprawdzić tabelę 9 konsekwencji oraz kolejność gałęzi na własnych fixture'ach, zamiast wierzyć moim.
- [ ] manifest pokrycia dowodu — audyt `C/M/E` (5 reguł: kanon-przed-egzekucją · rekord per artefakt wykonania · `E` append-only z etykietą rundy · proza bez kwantyfikatorów pokrycia · typy MEASURED/CONTRACTED). Dla KAŻDEGO zdania powołującego się na URUCHOMIONY dowód odczytaj zbiór WYKONANY z samego dowodu, nie z manifestu. Kompletność wyrażona LICZBĄ („11/11", „9 wierszy") liczy się tak samo jak „każdy/wszystkie".
- [ ] SQL/RLS/migracja                          ← N/A jeśli DB nietykane (spec jest read-only)
- [ ] UI/a11y/tokeny                            ← jeśli UI
- [ ] ryzyko/dane                              ← jeśli Risk: HIGH (L6)
- [ ] docs/proza spójność                      ← jeśli docs-only

Werdykt: APPROVE / NITS / NITS-EXT / REQUEST_CHANGES (linia **Werdykt:** w sekcji ## Werdykt).
Przy RC napisz, ile kategorii sprawdziłeś i które N/A.

PAMIĘTAJ:
- Review warstwowy (R1 A, R2 B, R3 C z TEGO SAMEGO pliku) = PORAŻKA procesu. Jeden głęboki przejazd.
- Wątpliwość → REQUEST_CHANGES. NITS = ≤20 LOC, brak nowych funkcji/architektury.
- NITS-EXT (≤50 LOC) dozwolony TEŻ na review SPECA dla mechanicznych/lokalnych poprawek. Wystawiasz NITS-EXT → DOŁĄCZ sekcję `## Self-fix uzasadnienie` (literalny format niżej). Bez kompletu pól → nieważny → REQUEST_CHANGES.
- Denylist (ZAWSZE RC): nowe kryterium akceptacji, nowy plik/komenda/faza/widok, decyzja architekt./produktowa, cross-faza, bullet z nowym scope, diff którego źródło NIE autoryzuje.
- Proactive suggestions w formacie parsowalnym (master 4.6). Final footer wg master 4.7.

Format `## Self-fix uzasadnienie` (TYLKO przy NITS-EXT, komplet 5 pól, każde dokładnie raz):
## Self-fix uzasadnienie
- change-class: <stale-ref|typo-contract|restore-required-section|regex-anchor-fix|fact-correction>
- files/hunks: <plik:od-do|plik:linia>[, <plik:od-do|plik:linia> ...]
- source: <path:line>
- source-root: <master|MEMORY|HANDOFF|Q-ANSWERS|code>
- LOC: <liczba>

Zapisz w docs/specs/md-export/_review/R3-codex.md.

[środowisko: Monorepo: tiolibri-frontend/ (React 19 + Vite + Tailwind 4), tiolibri-api/ (FastAPI, venv w tiolibri-api/venv). Kanon spec-workflow NIE jest w tym repo — leży w /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/ (czytaj stamtąd po ścieżce bezwzględnej). CME-MANIFEST.md nie istnieje w żadnym z repo — audyt C/M/E nie jest blokerem (reguła 1), ale bramka strukturalna preflightu obowiązuje.]
