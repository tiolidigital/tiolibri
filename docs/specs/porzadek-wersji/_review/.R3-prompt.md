Cześć Codex. R3 review docs/specs/porzadek-wersji/SPEC-PORZADEK-WERSJI-MASTER.md (wersja 0.4.1, status master-draft, Stadium A).

Przeczytaj NAJPIERW (w tej kolejności):
1. /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/SPEC-WORKFLOW-MASTER.md (sekcje 4.1, 4.4a, 4.4b, 4.6, 4.7, 9) — kanon spec-workflow NIE jest w tym repo, czytaj po ścieżce bezwzględnej. CME-MANIFEST.md nie istnieje w żadnym z repo, więc audyt C/M/E NIE jest blokerem werdyktu (reguła 1 „kanon przed egzekucją"), ale sprawdź go merytorycznie i zgłoś rozjazdy jako uwagi.
2. /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/RETRO.md oraz LESSONS.md w tym samym katalogu
3. docs/specs/porzadek-wersji/SPEC-PORZADEK-WERSJI-MASTER.md (plik pod review)
4. docs/specs/porzadek-wersji/_review/R1-codex.md, R1-opus-response.md, R2-codex.md, R2-opus-response.md (poprzednie rundy — WSZYSTKIE Twoje uwagi z R1 i R2 zostały przyjęte, 9/9 i 11/11) oraz R3-opus-preflight.md (preflight L5 tej rundy: 22 fakty, 4 CORRECTED, audyt C/M/E, self-test parsera U9)
5. docs/specs/porzadek-wersji/PROACTIVE-INBOX.md

Kontekst rundy: to jest OSTATNIA runda przed progiem eskalacji (Risk HIGH → MAX_ROUNDS=3, po R3 N_EFF=3). Delta 0.4 → 0.4.1 pochodzi wyłącznie z preflightu R3 i dotyczy WYKONALNOŚCI protokołu dowodu §6: zmierzone zostało, że deklarowany kanał (`tiolibri-api/venv/bin/python` + supabase-py) nie dosięga `pg_catalog` (PGRST205 na `pg_policies`) ani surowego SQL-a (PGRST202 ×4), nie ma `psql`, venv nie ma sterownika libpq, `.env` nie ma URL-a połączenia — więc §6.1 R1/R4 i całe §6.2 dostały drugi, nazwany kanał (Supabase SQL Editor). Dodatkowo §6.2 liczy md5 deklaracji triggera POZA transakcją, fixture dostał kolumny NOT NULL, a §1 odzyskało zmierzone `triggered_by`.

Zadanie: Review PRZED implementacją. Wylicz WSZYSTKIE problemy TEJ rundy naraz — NIE zostawiaj na kolejną. Widzisz warstwę B pod A — zgłoś OBIE teraz.
Przejdź exhaustiveness checklist. Dla KAŻDEJ kategorii [✓ sprawdzone] albo [N/A — jedno zdanie]. NIE wymyślaj pustego ceremoniału.
- [ ] budżet ROZMIARU (LOC/pliki/domeny/migracje — NIE liczba testów; §4.5)        ← ZAWSZE
- [ ] spójność z masterem + brak stale references                                   ← ZAWSZE
- [ ] egzekwowalność (spec → impl bez zgadywania?)                                   ← ZAWSZE
- [ ] poprawność logiki + edge/nullish        ← jeśli algorytm/logika
- [ ] bundling / mutation targets (1 it()=1 gałąź?)   ← TYLKO pure-logic test spec
- [ ] typy/sygnatury/argi/ścieżki istnieją    ← jeśli cytuje symbole/ścieżki
- [ ] bramki maszynowe URUCHOMIONE w sandboxie (komendy/regexy/fixture wykonane, podaj EXIT — NIE weryfikuj flag wobec docs; nieuruchamialna albo niewykonana bramka = RC). WYJĄTEK owner-attested DB evidence: bramka wymagająca Docker/DB/REST, której sandbox strukturalnie nie udźwignie — owner-attested plik z realnymi EXIT = akceptowalny dowód; jej niewykonanie u Ciebie bez Dockera NIE jest RC. Bramki wykonalne bez Dockera (parser U9 awk z /spec-health-check, regexy, fixtury shell) uruchom — ich false-PASS = RC   ← jeśli spec zawiera komendę/regex/fixture
- [ ] manifest pokrycia dowodu — audyt C/M/E: dla KAŻDEGO zdania powołującego się na URUCHOMIONY dowód odczytaj zbiór WYKONANY (E) z samego dowodu, nie z manifestu, i skonfrontuj z prozą (C) oraz manifestem (M). Kompletność wyrażona LICZBĄ („31 z 31", „12 projektów") i BEZ kwantyfikatora liczy się tak samo jak „każdy/wszystkie" — audyt adversarialny, nie grep listy słów. Kanon CME-MANIFEST nie istnieje → uwagi tak, bloker nie   ← spec cytuje uruchomione dowody
- [ ] SQL/RLS/migracja                          ← DOTYCZY (dwie migracje, dwa indeksy częściowe, trzy CHECK-i, polityki RLS, przepisany trigger kasujący dane)
- [ ] UI/a11y/tokeny                            ← DOTYCZY (§9)
- [ ] ryzyko/dane                              ← DOTYCZY (Risk HIGH, L6: trigger prune kasuje dane właściciela, twardy DELETE projektów w §7/§7.1)
- [ ] docs/proza spójność                      ← jeśli docs-only

Szczególnie proszę o adversarialny atak na trzy rzeczy, bo to one urosły w tej rundzie:
(a) §6.0/§6.1/§6.2 — czy podział na kanał A/B jest KOMPLETNY (czy każdy krok da się wykonać kanałem, do którego został przypisany) i czy artefakt kanału B (owner-attested, bez automatycznego kodu wyjścia) jest dowodem, a nie deklaracją;
(b) §6.2 — arytmetyka trzech przebiegów (GREEN 15 / mutacja #1 przypięty znika / mutacja #2 = 14 nieprzypiętych) wobec REALNEJ deklaracji triggera w tiolibri-frontend/docs/migrations/20260421_spec1.sql:61-79 oraz izolacja mutanta przez transakcyjny DDL;
(c) §3.1.1/§3.1.2 — czy CHECK `projects_book_canon` (regex postaci kanonicznej) plus DWA indeksy częściowe faktycznie domykają inwariant „jedna AKTUALNA", w tym dla `book IS NULL`, i czy kontrakt helpera §3.2 nie zostawia dziury dla ścieżki importu §3.6.1.

Werdykt: APPROVE / NITS / NITS-EXT / REQUEST_CHANGES (linia **Werdykt:** w sekcji ## Werdykt).
Przy RC napisz, ile kategorii sprawdziłeś i które N/A.

PAMIĘTAJ:
- Review warstwowy (R1 A, R2 B, R3 C z TEGO SAMEGO pliku) = PORAŻKA procesu. Jeden głęboki przejazd.
- Wątpliwość → REQUEST_CHANGES. NITS = ≤20 LOC, brak nowych funkcji/architektury.
- NITS-EXT (≤50 LOC) dozwolony TEŻ na review SPECA dla mechanicznych/lokalnych poprawek. Wystawiasz NITS-EXT → DOŁĄCZ sekcję `## Self-fix uzasadnienie` (literalny format niżej). Bez kompletu pól → nieważny → REQUEST_CHANGES.
- Denylist (ZAWSZE RC): nowe kryterium akceptacji, nowy plik/komenda/faza/widok, decyzja architekt./produktowa, cross-faza, bullet z nowym scope, diff którego źródło NIE autoryzuje.
- Proactive suggestions w formacie parsowalnym (master 4.6). Final footer wg master 4.7.

Format `## Self-fix uzasadnienie` (TYLKO przy NITS-EXT, komplet 5 pól, każde dokładnie raz):
## Self-fix uzasadnienie
- change-class: <stale-ref|typo-contract|restore-required-section|regex-anchor-fix|fact-correction>
- files/hunks: <plik:od-do|plik:linia>[, <plik:od-do|plik:linia> ...]
- source: <path:line>
- source-root: <master|MEMORY|HANDOFF|Q-ANSWERS|code>
- LOC: <liczba>

Zapisz w docs/specs/porzadek-wersji/_review/R3-codex.md.

[środowisko: Monorepo: tiolibri-frontend/ (React 19 + Vite + Tailwind 4), tiolibri-api/ (FastAPI, venv w tiolibri-api/venv). Kanon spec-workflow NIE jest w tym repo — leży w /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/ (czytaj stamtąd po ścieżce bezwzględnej). CME-MANIFEST.md nie istnieje w żadnym z repo — audyt C/M/E nie jest blokerem (reguła 1), ale bramka strukturalna preflightu obowiązuje.]
