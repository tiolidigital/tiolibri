Cześć Codex. R4 review docs/specs/porzadek-wersji/SPEC-PORZADEK-WERSJI-MASTER.md (wersja 0.5.1, status master-draft / spec: R4-codex-pending).

Przeczytaj NAJPIERW (w tej kolejności):
1. /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/SPEC-WORKFLOW-MASTER.md (sekcje 4.1, 4.4a, 4.4b, 4.6, 4.7, 9). CME-MANIFEST.md NIE ISTNIEJE w żadnym repo — zgodnie z regułą 1 audyt C/M/E nie jest blokerem, ale bramka strukturalna preflightu obowiązuje.
2. /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/RETRO.md
3. docs/specs/porzadek-wersji/SPEC-PORZADEK-WERSJI-MASTER.md
4. docs/specs/porzadek-wersji/_review/R1-codex.md, R2-codex.md, R3-codex.md oraz R1/R2/R3-opus-response.md (poprzednie rundy), _review/SPIKE-R3-kanal.md (zapis spike'u kanału dowodowego), _review/R4-opus-preflight.md (preflight L5 tej rundy)
5. docs/specs/porzadek-wersji/PROACTIVE-INBOX.md

KONTEKST TEJ RUNDY — przeczytaj zanim zaczniesz:
Ten spec przeszedł R1/R2/R3 (8+8+8 blokerów) i został ESKALOWANY na STOP-and-SPIKE: pięć uwag R3 to był ten sam rdzeń — protokół dowodu §6 deklarował kanał, którego na tej maszynie nie było. Spike ZDOBYŁ i ZMIERZYŁ kanał (Supabase Management API POST /v1/projects/{ref}/database/query, autoryzacja PAT-em z tiolibri-api/.env). §6 została napisana RAZ, pod pomiar. `reset-po-spike: R3` — budżet rund liczy się od nowa.
Masz w sandboxie ten sam kanał co ja: `tiolibri-api/venv/bin/python`, PAT w `tiolibri-api/.env` pod SUPABASE_ACCESS_TOKEN, ref bazy `klhnyagtobgtxnexdsls`. Skrypty pomiarowe, które możesz uruchomić albo przerobić: `_review/.R4-probe-ksztalt.py` (11 kroków, EXIT=0), `_review/.R4-probe-selftest.py` (kontrpróba bramki S4, EXIT=0), `_review/.R3-probe2-kanal.py`, `_review/.R3-probe3-rls.py`. NIE wierz moim wynikom — odpal je albo napisz własne. Wszystko, co pisze do bazy, MUSI kończyć się ROLLBACK-iem: to jest baza PRODUKCYJNA z dwiema książkami dwóch autorek.

Zadanie: Review PRZED implementacją. Wylicz WSZYSTKIE problemy TEJ rundy naraz — NIE zostawiaj na R5. Widzisz warstwę B pod A — zgłoś OBIE teraz.
Przejdź exhaustiveness checklist. Dla KAŻDEJ kategorii [✓ sprawdzone] albo [N/A — jedno zdanie]. NIE wymyślaj pustego ceremoniału (np. „1 it()=1 gałąź?" dla docs-only = N/A).
- [ ] budżet ROZMIARU (LOC/pliki/domeny/migracje — NIE liczba testów; §4.5)        ← ZAWSZE
- [ ] spójność z masterem + brak stale references                                   ← ZAWSZE
- [ ] egzekwowalność (spec → impl bez zgadywania?)                                   ← ZAWSZE
- [ ] poprawność logiki + edge/nullish        ← jeśli algorytm/logika
- [ ] bundling / mutation targets (1 it()=1 gałąź?)   ← TYLKO pure-logic test spec
- [ ] typy/sygnatury/argi/ścieżki istnieją    ← jeśli cytuje symbole/ścieżki
- [ ] bramki maszynowe URUCHOMIONE w sandboxie (komendy/regexy/fixture wykonane, podaj EXIT — NIE weryfikuj flag wobec docs; nieuruchamialna albo niewykonana bramka = RC). Konkretnie: kanał dowodowy §6.0 JEST w tym sandboxie osiągalny (patrz KONTEKST wyżej) — uruchom kroki §6.0a C1-C7 i te kroki §6.1/§6.1r/§6.2, które da się odpalić PRZED migracją (asercje na pg_policies, hash pg_get_functiondef, kształt bloku DO, zachowanie json_agg), zamiast wierzyć moim pomiarom. **WYJĄTEK owner-attested DB evidence** (wyjątek lokalny tej komendy — w masterze go NIE ma)**:** bramka wymagająca Docker/DB/REST, której sandbox recenzenta strukturalnie nie udźwignie — owner-attested plik z realnymi EXIT (`_review/…-evidence.md`, `PASS=N FAIL=0`) = akceptowalny dowód; jej niewykonanie u recenzenta bez Dockera **NIE** jest RC. Bramki wykonalne bez Dockera (fixtury shell/regexy/parsery) recenzent nadal uruchamia — ich false-PASS = RC   ← jeśli spec zawiera komendę/regex/fixture
- [ ] manifest pokrycia dowodu — audyt `C/M/E` (kanon nie istnieje w tym repo → nie jest blokerem werdyktu, ale sprawdź merytorycznie). Dla KAŻDEGO zdania powołującego się na URUCHOMIONY dowód: odczytaj zbiór WYKONANY (`E`) **z samego dowodu**, nie z manifestu, i skonfrontuj z prozą (`C`) oraz manifestem (`M`) w `_review/R4-opus-preflight.md`. Kompletność wyrażona LICZBĄ („11/11", „12 z 12", „31 snapshotów") i BEZ kwantyfikatora liczy się tak samo jak „każdy/wszystkie" — to audyt adversarialny, nie grep listy słów   ← jeśli spec cytuje uruchomiony dowód
- [ ] SQL/RLS/migracja                          ← jeśli DB
- [ ] UI/a11y/tokeny                            ← jeśli UI
- [ ] ryzyko/dane                              ← jeśli Risk: HIGH (L6)
- [ ] docs/proza spójność                      ← jeśli docs-only

Werdykt: APPROVE / NITS / NITS-EXT / REQUEST_CHANGES (linia **Werdykt:** w sekcji ## Werdykt).
Przy RC napisz, ile kategorii sprawdziłeś i które N/A.

PAMIĘTAJ:
- Review warstwowy (R1 A, R2 B, R3 C z TEGO SAMEGO pliku) = PORAŻKA procesu. Jeden głęboki przejazd. Cel: 3 RC → 1.
- Wątpliwość → REQUEST_CHANGES. NITS = ≤20 LOC, brak nowych funkcji/architektury.
- NITS-EXT (≤50 LOC) dozwolony TEŻ na review SPECA dla mechanicznych/lokalnych poprawek. Wystawiasz NITS-EXT → DOŁĄCZ sekcję `## Self-fix uzasadnienie` (literalny format niżej). Bez kompletu pól → nieważny → REQUEST_CHANGES.
- Denylist (ZAWSZE RC): nowe kryterium akceptacji, nowy plik/komenda/faza/widok, decyzja architekt./produktowa, cross-faza, bullet z nowym scope, diff którego źródło NIE autoryzuje.
- Proactive suggestions w formacie parsowalnym (master 4.6). Final footer wg master 4.7.

Format `## Self-fix uzasadnienie` (TYLKO przy NITS-EXT, komplet 5 pól, każde dokładnie raz):
## Self-fix uzasadnienie
- change-class: <stale-ref|typo-contract|restore-required-section|regex-anchor-fix|fact-correction>
- files/hunks: <plik:od-do|plik:linia>[, <plik:od-do|plik:linia> ...]
- source: <path:line>
- source-root: <master|MEMORY|HANDOFF|Q-ANSWERS|code>
- LOC: <liczba>

Zapisz w docs/specs/porzadek-wersji/_review/R4-codex.md.

[środowisko: Monorepo: tiolibri-frontend/ (React 19 + Vite + Tailwind 4), tiolibri-api/ (FastAPI, venv w tiolibri-api/venv). Kanon spec-workflow NIE jest w tym repo — leży w /Users/piotrmichalski/Documents/SaaS_Factory2026/FABRYKA/docs/specs/spec-workflow/ (czytaj stamtąd po ścieżce bezwzględnej). CME-MANIFEST.md nie istnieje w żadnym z repo — audyt C/M/E nie jest blokerem (reguła 1), ale bramka strukturalna preflightu obowiązuje.]
